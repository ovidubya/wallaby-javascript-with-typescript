import SomethingBrowser from '../src/SomethingBrowser';


describe('when somethingbrowser is imported', () => {
    it('it should just log stuff to the console ', () => {
        SomethingBrowser.printData();
    });
})